TODO:
- HorizontalListBox: add option to make every item the same width
- All classes should have a copy constructor and an
  operator=().
- purple_pounces_load
- better error catching in main
- grep for todo's and fix them
- add const in a lot of places
- put everything (in cppconsui) in a CppConsUI namespace ?
- put something in: README AUTHORS ChangeLog NEWS
- make sure default constructors are not callable for all
  classes where this applies
- Add an init function to cppconsui which calls
  bindtextdomain (PACKAGE, LOCALEDIR); (as explained by
  <http://www.gnu.org/software/gettext/manual/gettext.html#Libraries>
  also make the translations proper)
- add function descriptions in the header files
- colorscheme should use same concept as KeyConfig so it is possible to get
  all registered colorschemes at startup time
- options window, for the start with debug options
- statusbar and top-statusbar as known from cim4
- TextEntry -- hide passwords with asterisks, fix cursor movement that goes
  outside the area
- pending auth and auth request windows
- request UI ops implementation
- notice UI ops implementation
- fix and put back cmake build
- chat history
- buddy list customization
- status changing (review)
- buddy info window
- options window
- TreeView: add PgUp/PgDn binds, add quick search
- check if AC_GNU_SOURCE can be removed from configure.ac
- handle PURPLE_DEFAULT_ACTION_NONE correctly
- rename "Widget& widget" to "Widget& activator"
- ComboBox improvements
- don't create ChoiceDialog (Request UI) if there are no options
- include complete libpurple instead of individual files?
- cppconsui, InitLibrary() function
- src destructors -- purple_set_ui_ops(NULL)?
- GeneralMenu autosize -> MenuWindow autosize
- SIGWINCH bug fix
- non UTF-8 fix
- fix Conversation history overflow
- purple_blist_get_group_size(), purple_blist_get_group_online_count()
- gtkblist.c: buddy_is_displayable(), redo_buddy_list()
- remove SetCanFocus()?
- TimeoutConnect replacement
- TreeView/HorizontalListBox/ListBox -- optimize drawing, don't call
  MoveResize() if not necessary
- Window::SetVisibility() -- hide in protected
- add profiling (gprof, sprof, oprofile), debuging info into HACKING
- make SetInputChild() virtual, set focus_child inside it
- CoreManager shouldn't register FreeWindow::ScreenResized() callback, Window
  should do that
- TextEdit: horizontal tab characters..

Less important todo's which sometimes are just ideas:
- Add option to save/display buddy icon
- Use the fact noted about in g_utf8_offset_to_pointer doc.
